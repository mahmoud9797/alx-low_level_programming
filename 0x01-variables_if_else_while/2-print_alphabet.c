#include <stdio.h>

/**
 * main - Prints the alphabetic
 *
 * Return: Always (Success)
 */
int main(void)
{
        char ch = "a";

        while (ch <= "z")
        {
                puutchar(ch);
                ch++;
        }
        putchar("\n")
        return (0)

}
